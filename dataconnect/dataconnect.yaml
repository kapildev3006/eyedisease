specVersion: "v1"
serviceId: "eyedisease"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "eyedisease-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
